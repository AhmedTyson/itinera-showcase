export const ARCH_DATA: Record<string, { title: string; html: string }> = {
  client: { title: "Client Layer — Frontend", html: `<p class="muted" style="font-size:13.5px;line-height:1.7">Vanilla multi-page frontend (355 files) — 48+ HTML pages, 33 JS modules. No SPA framework, no bundler. Talks to <code class="mono">/api/*</code> with JWT bearer from localStorage.</p><div class="divider"></div><dl class="kv"><dt>Stack</dt><dd>HTML + CSS + Vanilla JS (common.js, catalog-*.js, checkout.js, chat.js)</dd><dt>Pages</dt><dd>/, explore, destinations, hotels, flights, app/*, admin/* (11), agency/* (7), auth/* (8)</dd><dt>State</dt><dd>localStorage token + user, DOM-driven</dd><dt>Responsive</dt><dd>Mobile-first utility CSS, glassmorphic dark theme</dd></dl>` },
  api: { title: "API Gateway — Laravel Router", html: `<p class="muted" style="font-size:13.5px"><code class="mono">routes/api.php</code> — 213 API routes (222 total). Domain-grouped: Account, Catalog, Trips, Commerce, System. Throttles per surface, CORS allowlist, signed URLs, Scramble OpenAPI at <code class="mono">/docs/api</code>.</p><div class="divider"></div><ul style="margin:0;padding-left:18px;color:var(--muted);font-size:13px;line-height:1.8"><li>throttle: login · register · ai · checkout · weather · maps · contacts · newsletter · refresh</li><li><code class="mono">/api/v1/*</code> compatibility aliases for public catalog</li><li>Fallback route → SPA-friendly 404 handling</li></ul>` },
  auth: { title: "Authentication — JWT + RBAC", html: `<p class="muted" style="font-size:13.5px"><code class="mono">tymon/jwt-auth</code> guard <code class="mono">api</code> + <code class="mono">spatie/laravel-permission</code>. User implements <code class="mono">MustVerifyEmail</code>; signed verify link redirects browsers to the frontend success page.</p><div class="divider"></div><dl class="kv"><dt>Guard</dt><dd>Bearer JWT · auth:api · verified middleware gate</dd><dt>Roles</dt><dd>super_admin · admin · agency · user (+ per-route permissions)</dd><dt>Tokens</dt><dd>Refresh rotation (throttle 15/min), blacklist on logout</dd><dt>OAuth</dt><dd>Google + Facebook via Socialite; email-link verification always required (no auto-trust of provider flag)</dd></dl>` },
  controllers: { title: "Controllers — 49", html: `<p class="muted" style="font-size:13.5px">Thin controllers over fat services; Admin/Public split per domain. Example: <code class="mono">TripController@store</code> validates via <code class="mono">StoreTripRequest</code>, delegates to <code class="mono">TripService</code>, returns <code class="mono">ApiResponse::success(new TripResource(...), 201)</code>.</p>` },
  services: { title: "Services — 28", html: `<p class="muted" style="font-size:13.5px">Business core: TripService · GroqService · PaymobGateway/PaymobClient · CheckoutService + StrategyFactory (Subscription | TripFork | TripPackage) · WebhookService · PriceCalculatorService · AiUsageService · fixture/geocode services.</p>` },
  repos: { title: "Repositories — 19 pairs", html: `<p class="muted" style="font-size:13.5px">Interface + implementation bound in <code class="mono">AppServiceProvider</code>: TripRepository, UserRepository, PaymentRepository, PlanRepository… keeps Eloquent behind contracts for testability/swapping.</p>` },
  models: { title: "Models — 37 across 6 domains", html: `<p class="muted" style="font-size:13.5px">Account(User, Role) · Catalog(Destination, Hotel, Restaurant, Attraction, Flight, Category, Country, Region) · Trips(Trip, ItineraryItem, TripDestination, AiGeneration…) · Commerce(Order, Payment, Plan, Subscription, AgencyAssignment…) · System(Setting, Flag, Report, Survey, Notification…) · Chat(Conversation, Message). Polymorphic <code class="mono">trip_items</code> morphToMany; morph map enforced.</p>` },
  db: { title: "Database — 44 migrations", html: `<p class="muted" style="font-size:13.5px">MySQL in prod (SQLite dev/test). SoftDeletes on majors, enum casts (TripStatus…), pivot <code class="mono">trip_destinations</code>, polymorphic <code class="mono">trip_items</code> + denormalized <code class="mono">itinerary_items</code> day planner, payments/orders ledger, ai_generations audit trail.</p>` },
  ai: { title: "AI — Groq llama-3.3-70b", html: `<p class="muted" style="font-size:13.5px">Pipeline: AiTripRequest validation → md5 cache key (60m) → quota consumed inside cache-miss closure only → Groq chat completion (max_tokens ≈ days×800, capped 32k) → JSON strip/parse → enrichPlanWithCatalog() binds real hotel/restaurant/attraction IDs + coords → deterministic luxury fallback engine so generation never hard-fails.</p>` },
  pay: { title: "Payments — Paymob", html: `<p class="muted" style="font-size:13.5px">Intention API → clientSecret → hosted unifiedcheckout → HMAC-verified webhook + callback. Production fail-fast if PAYMOB_HMAC empty. Bounded cURL timeouts (30s/5s) via PaymobClient override. Idempotency by merchant_order_id; FulfillOrderListener settles orders/subscriptions/forks.</p>` },
  ext: { title: "External Integrations", html: `<ul style="margin:0;padding-left:18px;color:var(--muted);font-size:13px;line-height:1.8"><li><b>Open-Meteo</b> — weather (throttle:weather, cached; abuse-tested)</li><li><b>OpenStreetMap/Nominatim</b> — geocoding + city/country/flight fixtures seeding</li><li><b>Mail</b> — Welcome, TripBooked/Forked, PaymentSuccess/Failed, BookingCancelled, SubscriptionActivated, ReviewPublished/Flagged</li><li><b>DomPDF + OpenSpout</b> — PDF/XLSX reports via queued jobs</li></ul>` },
  queue: { title: "Queue & Jobs", html: `<p class="muted" style="font-size:13.5px">database driver default (redis-ready). Jobs: GenerateReportJob, GeocodeDestinationJob. Listeners: FulfillOrderListener, HandlePaymentFailed. supervisor keeps <code class="mono">queue:listen</code> alive in Docker.</p>` },
  obs: { title: "Observability", html: `<p class="muted" style="font-size:13.5px">Laravel Pail realtime logs (dev), Telescope opt-in via TELESCOPE_ENABLED (prod-gated), stack/single channels, framework health at <code class="mono">GET /up</code>. No APM yet — documented gap.</p>` },
}

export const ENT_DATA: Record<string, { title: string; html: string }> = {
  user: { title: "users — Account Core", html: `<p class="muted">Auth + profile + roles. google_id/facebook_id OAuth columns, is_active/is_blocked flags, email_verified_at gate field.</p><div class="code">users: id · name · email(unique) · password(hashed) · phone · is_active · is_blocked · email_verified_at · google_id · facebook_id · address</div>` },
  trip: { title: "trips — Root Aggregate", html: `<p class="muted">SoftDeletes. status enum cast; fork lineage via parent_trip_id/original_trip_id/is_fork; is_public enables community forking.</p><div class="code">trips: belongsTo user · belongsToMany destinations(pivot trip_destinations w/ day_number,visit_order) · morphToMany hotels/restaurants/attractions/flights (trip_items) · hasMany itinerary_items, ai_generations</div>` },
  dest: { title: "destinations", html: `<p class="muted">Catalog hub with lat/lng for maps; region/country FK chain powers continent pills on explorer.</p>` },
  hotel: { title: "hotels", html: `<p class="muted">price_per_night feeds attach estimated_cost; destination_id scope used by AI enrichment round-robin.</p>` },
  attraction: { title: "attractions", html: `<p class="muted">POIs with entry_fee + coords; AI itinerary items bind itemable_id here when type matches.</p>` },
  restaurant: { title: "restaurants", html: `<p class="muted">average_price + cuisine; same polymorphic attach path as hotels/attractions.</p>` },
  flight: { title: "flights", html: `<p class="muted">origin/destination + price; boarding-pass detail page renders FlightResource fields verbatim.</p>` },
  itinerary: { title: "itinerary_items — Day Planner", html: `<p class="muted">Denormalized planner rows created from AI days[] or manual attach. Unique-per-trip ordering by (day_number,item_order).</p><div class="code">itinerary_items: trip_id · itemable_type/id(nullable poly) · day_number · item_order · type · time_slot · title · notes · estimated_cost · latitude/longitude · location_label</div>` },
  payment: { title: "orders + payments — Ledger", html: `<p class="muted">orders(reference unique, total, currency, status) ← order_items(poly) ; payments(order, gateway, amount_cents, payload json). Webhook HMAC verifies before any state transition.</p>` },
  agency: { title: "agency_assignments — Marketplace", html: `<p class="muted">trip+agency+client triangle; status machine pending→approved→completed/declined/cancelled; earnings feed agency dashboard; policies enforce role scoping.</p>` },
  ai_gen: { title: "ai_generations — Audit Trail", html: `<p class="muted">Per-generation record (prompt/response/model/tokens) powering quota reporting and cache-hit analytics (AiQuotaCacheHitTest).</p>` },
  review: { title: "reviews + flags — Moderation", html: `<p class="muted">Polymorphic reviewable; admin approve/reject pipeline; flags route to AdminFlagController with role gate.</p>` },
}
